
**File 5: docs/self-hosting.md** (ready for your home server)
```markdown
# Self-Hosting temper on your KUbuntu home server

Recommended: Forgejo (lightweight Gitea fork)

1. Install Forgejo (docker-compose)
2. `git remote set-url origin git@your-server:jak/temper.git`
3. Push
4. Run CI locally with `scripts/ci.sh`

(Full docker-compose.yml and instructions will be expanded as we go.)