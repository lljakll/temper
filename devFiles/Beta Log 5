#34 - 2026-09-13 - Budget - Enhancement - Low
You are working in the **Beta Thread**.

Task: Default-sort Budget lines by CoA ascending on load and refresh.

Requirements:
1. When a budget is opened, created, or refreshed (desktop table and mobile line list), display lines in CoA number ascending order.
2. Do not add column filters, search, or a mobile Sort button in this change.
3. Do not change Remaining, duplicate, activate/close, or save behavior. Sort is display order (and persist order only if the app already rewrites line order on save — do not invent a new stored sort column unless required to keep the display stable).
4. Lines without a CoA should sort last.

First explore how budget lines are loaded and rendered, then apply CoA ascending as the default order.

Begin now.

---
Versioning & Schema Mandate (required):
- Increment the application version.
- If any schema change is required, generate a proper schema patch using the current filename format (YYYYMMDD_<appversion_without_decimal>_description.sql) with full metadata header.
- Record the new app version + required schema version in the app_version table of the current working database.
- Update VERSION.md with the new version entry and the “SCHEMA UPDATE REQUIRED” note if a patch is generated. If no schema change is needed, still update VERSION.md with the new version and a clear note of the change.


#35 - 2026-09-14 - Ledger / Column filters - Bug - High

You are working in the **Beta Thread**.

Task: Fix Ledger column filters failing (or applying incompletely) once the ledger has more than about 600 transactions.

Requirements:
1. Diagnose why filters break or ignore data above ~600 rows (client-only unique lists, list API page size, a hard LIMIT, first-page-only apply, etc.).
2. Filters must run against the **full** transaction set, not only the rows already loaded for infinite scroll.
3. Unique-value lists in the filter dropdown must include values from the whole dataset (or a correct server-side distinct list), not only the first ~600 loaded rows.
4. Apply / search / select-all / clear must stay consistent with that full-set behavior.
5. Do not regress infinite scroll, sort, or current Excel-style filter UX.
6. Do not introduce a new pagination scheme for the main list.

First explore the ledger list API, how filter unique values are built, and any 500/600-row limits, then remove the cap so filters work past 600 entries.

Begin now.

---
Versioning & Schema Mandate (required):
- Increment the application version.
- If any schema change is required, generate a proper schema patch using the current filename format (YYYYMMDD_<appversion_without_decimal>_description.sql) with full metadata header.
- Record the new app version + required schema version in the app_version table of the current working database.
- Update VERSION.md with the new version entry and the “SCHEMA UPDATE REQUIRED” note if a patch is generated. If no schema change is needed, still update VERSION.md with the new version and a clear note of the change.


#36 - 2026-09-15 - Ledger - Enhancement - Med

You are working in the **Beta Thread**.

Task: Add bulk delete of pending transactions so a date-filtered historical import can be removed in one pass.

Requirements:
1. Bulk delete uses existing Ledger multi-select.
2. Provide a reliable way to select every transaction that matches the **current filters** (e.g. Select all filtered), not only the rows loaded on screen. This is required so a full year (e.g. all of 2023) can be deleted after a date filter without clicking each row.
3. Same rules as single delete:
   - Only pending / not cleared / not reconciled
   - Admin and Treasurer only
   - Confirmation shows how many will be deleted and a sample of identifying fields (date, ref, amount)
   - Required Reason for delete (one reason for the batch)
   - Remove header, lines, documents, and attachment files
   - audit_log: user, active role, timestamp, reason, and either one row per txn or one batch row that lists every ref #
4. Cleared or reconciled rows in the selection/filter must be skipped or must abort the batch with a clear count. Never delete them.
5. Do not add a special “delete fiscal year including cleared” command. Date filter + select filtered + bulk delete is the mechanism.
6. Do not regress single delete, filters, or infinite scroll.

First explore pending-delete, multi-select, filter apply, and how “select all” currently works (loaded page vs full filter set), then implement bulk delete as specified.

Begin now.

---
Versioning & Schema Mandate (required):
- Increment the application version.
- If any schema change is required, generate a proper schema patch using the current filename format (YYYYMMDD_<appversion_without_decimal>_description.sql) with full metadata header.
- Record the new app version + required schema version in the app_version table of the current working database.
- Update VERSION.md with the new version entry and the “SCHEMA UPDATE REQUIRED” note if a patch is generated. If no schema change is needed, still update VERSION.md with the new version and a clear note of the change.


#38 - 2026-09-15 - Dashboard - Enhancement - Low

You are working in the **Beta Thread**.

Task: Add a proper header to the Dashboard that uses the configured church name and icon.

Requirements:
1. Give the Dashboard a clear page header consistent with other main screens (Ledger, Budget, Funds).
2. Use the System Configuration church display name and icon/graphic when they are set.
3. Header should identify the app/home at a glance (church name plus a short subtitle such as Dashboard or current budget year if that is already available and simple).
4. Work on desktop and mobile. Do not crowd the mobile top bar; keep it readable.
5. Do not rebuild dashboard cards or add new metrics in this change.
6. Do not change System Configuration except to read the existing name/icon settings.

First explore the Dashboard page shell and how church name/icon are stored and shown in the sidebar/tab, then add the Dashboard header.

Begin now.

---
Versioning & Schema Mandate (required):
- Increment the application version.
- If any schema change is required, generate a proper schema patch using the current filename format (YYYYMMDD_<appversion_without_decimal>_description.sql) with full metadata header.
- Record the new app version + required schema version in the app_version table of the current working database.
- Update VERSION.md with the new version entry and the “SCHEMA UPDATE REQUIRED” note if a patch is generated. If no schema change is needed, still update VERSION.md with the new version and a clear note of the change.


#39 - 2026-09-16 - Budget / Reports - Enhancement - Med

You are working in the **Beta Thread**.

Task: Export a budget to CSV and PDF.

Requirements:
1. Add Budget export to the Reports screen: choose budget, choose format (CSV or PDF), download.
2. Also add Export on the Budget page (desktop control and mobile flyout) that exports the budget currently being viewed, same formats.
3. CSV includes at least CoA, account name, budgeted amount, and Remaining if that value is already calculated. One row per budget line. Include budget name and period in a header row or obvious first rows.
4. PDF is a simple readable document: church name if configured, budget name, fiscal period, status, total, then the line list. Meeting-usable, not a designed annual report.
5. Restrict to roles that can already view that budget.
6. Do not build a multi-report board packet or change budget edit/activate logic.

First explore Reports, Budget view, and any existing export/PDF helpers, then add CSV and PDF budget export in both places.

Begin now.

---
Versioning & Schema Mandate (required):
- Increment the application version.
- If any schema change is required, generate a proper schema patch using the current filename format (YYYYMMDD_<appversion_without_decimal>_description.sql) with full metadata header.
- Record the new app version + required schema version in the app_version table of the current working database.
- Update VERSION.md with the new version entry and the “SCHEMA UPDATE REQUIRED” note if a patch is generated. If no schema change is needed, still update VERSION.md with the new version and a clear note of the change.
