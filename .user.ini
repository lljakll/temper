; CGI/FPM fallback (mod_php reads .htaccess php_value instead).
; Raised for backup-package restore (DB dump + attachments). Ledger attachments stay capped in code.
upload_max_filesize = 128M
post_max_size = 160M

